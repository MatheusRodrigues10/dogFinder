import { LoaderContainer, LoadingSpinner } from "./styles"

const Loader = () => {
    return (
        <LoaderContainer>
            <LoadingSpinner />
        </LoaderContainer>
    )
};

export default Loader;